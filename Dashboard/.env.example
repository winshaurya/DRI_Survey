VITE_DATABASE_URL=postgresql://postgres:{password}@URL.supabase.co:5432/postgres
# Notes:
# - This project is configured to derive the PostgREST (Supabase REST) host and
#   REST headers from `VITE_DATABASE_URL` at runtime in the browser.
# - Browsers cannot open raw Postgres sockets; the frontend talks to the
#   project's REST endpoint (https://<project>.supabase.co/rest/v1).
# - Security warning: exposing DB credentials or service keys in client-side
#   env is unsafe for production. Prefer a server-side proxy or provide only
#   a publishable anon key when necessary.