VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Server-side (DO NOT COMMIT — store these in Vercel/Secrets)
# SUPABASE_SERVICE_ROLE_KEY=
# SUPABASE_URL=
# Copy to `.env` locally for development (do NOT commit `.env`)
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# On Vercel set this as a project environment variable (do NOT prefix with VITE_)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Notes:
# - Use the anon key (`VITE_SUPABASE_ANON_KEY`) in client-side code.
# - Use the service role key (`SUPABASE_SERVICE_ROLE_KEY`) only inside serverless functions (Vercel Environment Variables).